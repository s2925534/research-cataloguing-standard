# OS / IDE
.DS_Store
.idea/
.claude/
.ai/
__pycache__/
*.pyc

# This checkout's real config, generated schema, and cataloguing outputs.
# templates/ holds the committed, reusable, project-agnostic versions instead.
instance/
updatesPrompt.txt

# Personal/non-research files purged from the catalogue (resumes, cover
# letters, other job-application material, credential recovery codes) -
# kept locally for whoever runs this tool, never tracked by git.
unrelated_to_thesis/

# review.py's own pre-save safety snapshots, its citation follow-up report,
# and migrate_legacy_to_dsr.py's test-conversion preview output - transient
# working-session artefacts from running the tools, not tool functionality
# itself, and can otherwise carry stale content into tracked history.
*.review-backup-*.md
*-proposed.md
*.dsr-test-conversion.md
citation_review_followups.md
